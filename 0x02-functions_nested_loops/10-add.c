#include <stdio.c>
/**
 * add - a function that add two number
 * a - int  parameter
 * b - int  parameter
 * Return: Always a + b
 */
int add(int a, int b);

/**
 * main - Entry point
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n;

	n = add(89, 9);
	printf("%d\n", n);
	return (0);
}
int add(int, int)
{
	return (a + b);
}

