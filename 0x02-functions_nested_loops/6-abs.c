#include "main.h"
/**
 * _abs - a function that computes the absolute value of an integer.
 * @n: the number tode abs
 * Return: the abs value
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
